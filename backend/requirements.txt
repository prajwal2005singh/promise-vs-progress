# Web framework
fastapi
uvicorn[standard]
python-multipart      # required by FastAPI for form/file uploads (evidence photos)

# Database
sqlalchemy

# Auth
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1          # passlib 1.7.4's backend detection breaks on bcrypt>=4.1
email-validator       # required by pydantic EmailStr used in auth schemas

# Validation
pydantic

# AI processor (ai_processor.py) + citizen evidence verification
# (services/verification_service.py) -- both reuse GOOGLE_API_KEY
google-genai
python-dotenv
pillow                 # EXIF GPS/timestamp extraction + image fingerprinting for evidence photos

# Scraper (scraper.py)
requests
beautifulsoup4

# Blockchain integration (blockchain_client.py)
web3

# Optional local CLIP routing for the Image Engine
torch
open_clip_torch
joblib
